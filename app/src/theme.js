import {DefaultTheme} from 'react-native-paper';

export default {
  ...DefaultTheme,
  dark: true,
  colors: {
    ...DefaultTheme.colors,
    // primary: 'tomato',
    // accent: 'yellow',
    text: 'white',
    background: '#222',
    surface: '#424242'
  },
}